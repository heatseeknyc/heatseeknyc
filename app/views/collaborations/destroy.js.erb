<% if !@collaboration.persisted? %>
  $("div.collaborators-sidebar").remove();
  $("div#append-collaborations").append('<%= j render_collaborators_for(current_user) %>');
<% else %>
  $(".alert").remove();
  $(".show-layout").prepend('<%= j render "delete_collaboration_failure_message" %>');
<% end %>