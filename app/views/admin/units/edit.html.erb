<%= render partial: "form",
           locals: {
             title: "Edit #{@unit.name} in #{@building.address_and_name}",
             resource: @unit,
             url: admin_building_unit_path(@building, @unit),
             action_label: "UPDATE"
           } %>
