<%= render partial: "form",
           locals: {
             title: "Edit #{@unit.name} in #{@building.address_and_name}",
             resource: @unit,
             url: edit_admin_building_unit_path(@building, @unit),
             action_label: "UPDATE"
           } %>
