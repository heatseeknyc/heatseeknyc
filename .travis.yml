language: ruby
rvm:
- 2.3.1
cache: bundler
env:
  - DATBASE_USERNAME="postgres"
  - DATABASE_PASSWORD=""
notifications:
  email: false
script:
  - RAILS_ENV=test bundle exec rake db:create
  - RAILS_ENV=test bundle exec rake db:test:prepare
  - bundle exec rspec spec
